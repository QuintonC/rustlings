// variables6.rs
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a hint.

const NUMBER: i32 = 3;
fn main() {
    // let test = 3i32;
    // note to self, constants must be typed on usage
    // lets can be scoped and strictly bound on declaration.

    println!("Number {}", NUMBER);
}
